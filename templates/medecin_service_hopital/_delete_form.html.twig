<form method="post" action="{{ path('medecin_service_hopital_delete', {'id': medecin_service_hopital.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ medecin_service_hopital.id) }}">
    <button class="btn">Delete</button>
</form>
