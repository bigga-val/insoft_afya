<form method="post" action="{{ path('choix_medecin_delete', {'id': choix_medecin.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ choix_medecin.id) }}">
    <button class="btn">Delete</button>
</form>
