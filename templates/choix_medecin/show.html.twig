{% extends 'base.html.twig' %}

{% block title %}ChoixMedecin{% endblock %}

{% block body %}
    <h1>ChoixMedecin</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ choix_medecin.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ choix_medecin.createdAt ? choix_medecin.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>CreatedBy</th>
                <td>{{ choix_medecin.createdBy }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ choix_medecin.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('choix_medecin_index') }}">back to list</a>

    <a href="{{ path('choix_medecin_edit', {'id': choix_medecin.id}) }}">edit</a>

    {{ include('choix_medecin/_delete_form.html.twig') }}
{% endblock %}
