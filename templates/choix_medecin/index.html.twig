{% extends 'baseAdmin.html.twig' %}

{% block title %}ChoixMedecin index{% endblock %}

{% block body %}
    <h1>ChoixMedecin index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>CreatedAt</th>
                <th>CreatedBy</th>
                <th>Status</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for choix_medecin in choix_medecins %}
            <tr>
                <td>{{ choix_medecin.id }}</td>
                <td>{{ choix_medecin.createdAt ? choix_medecin.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ choix_medecin.createdBy }}</td>
                <td>{{ choix_medecin.status }}</td>
                <td>
                    <a href="{{ path('choix_medecin_show', {'id': choix_medecin.id}) }}">show</a>
                    <a href="{{ path('choix_medecin_edit', {'id': choix_medecin.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('choix_medecin_new') }}">Create new</a>
{% endblock %}
