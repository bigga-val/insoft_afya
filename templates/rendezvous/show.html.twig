{% extends 'baseAdmin.html.twig' %}

{% block title %}Rendezvous{% endblock %}

{% block body %}
    <h1>Rendezvous</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rendezvou.id }}</td>
            </tr>
            <tr>
                <th>Motif</th>
                <td>{{ rendezvou.motif }}</td>
            </tr>
            <tr>
                <th>HeureRendezvous</th>
                <td>{{ rendezvou.HeureRendezvous ? rendezvou.HeureRendezvous|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ rendezvou.createdAt ? rendezvou.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>CreatedBy</th>
                <td>{{ rendezvou.createdBy }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('rendezvous_index') }}">back to list</a>

    <a href="{{ path('rendezvous_edit', {'id': rendezvou.id}) }}">edit</a>

    {{ include('rendezvous/_delete_form.html.twig') }}
{% endblock %}
